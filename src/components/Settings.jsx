import React from "react";
import "../styles/Settings.css";

const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <p>Placeholder for settings (SMTP configurations, etc.)</p>
    </div>
  );
};

export default Settings;
